# Local maintenance requires the matching operating-system account.
local all all peer
# Network clients must present an appliance-issued certificate; no password path.
hostssl platform_agent_memory cortex_app all cert
hostssl platform_agent_memory postgres all cert map=cortex_admin
